# Moved — see the root CLAUDE.md

This `site/` subdirectory is a legacy parallel copy from when the starter used a monorepo-y layout. The canonical project notes live at the repository root:

- In-repo: [`../CLAUDE.md`](../CLAUDE.md)
- The universal NGF foundation: https://raw.githubusercontent.com/Nick-NGFsystems/NGF-Systems-app/main/NGF-STANDARDS.md

Active development should happen at the repo root. If your fork still uses this subdir, consider flattening it: delete `site/`, set Vercel's Root Directory back to `.` (default), commit. Less drift, simpler structure.
